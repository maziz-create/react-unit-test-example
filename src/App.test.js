/*
NOT: BURASI TESTTE HATA ÇIKARTIYOR. 1 HATA VARSA ORASI BURASI.
import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  render(<App />); //App componentini render et.

  //doma bak içinde learn react yazan bi link var mı
  const linkElement = screen.getByText(/learn react/i);

  //bekle.. dökümanın içinde de bu link var mı ? burası true dönerse testi geçer.
  expect(linkElement).toBeInTheDocument();
});
*/
